# eerste regel inlezen
# OPMERKING: er mag nog niet van uitgegaan worden dat deze regel een cijfer
#            bevat, omdat deze regel ook het woord stop kan bevatten
x1 = input()

while x1 != 'stop':
    
    # volgende cijfers inlezen en controlecijfer berekenen
    controlecijfer = int(x1)
    for i in range(2, 10):
        controlecijfer += i * int(input())
    controlecijfer %= 11
    
    # controlecijfer testen
    print('OK' if controlecijfer == int(input()) else 'FOUT')
    
    # nieuwe regel inlezen
    # OPMERKING: er mag nog niet van uitgegaan worden dat deze regel een cijfer
    #            bevat, omdat deze regel ook het woord stop kan bevatten
    x1 = input()